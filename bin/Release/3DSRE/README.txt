Usage:
1. Open rom (if decrypted, you're fine, if not, make sure xorpad is '<product code>0.romfs.xorpad')
2. edit dumpted romfs
3. Hit Rebuild Romfs
4. ???
5. Profit

Other features:
 - Hitting Rebuild Romfs without loading a rom will allow you to select a romfs to rehash.